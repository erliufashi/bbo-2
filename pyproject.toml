[project]
name = "orchestrator"
version = "0.1.0"
description = "可插拔测试评估引擎参考实现"
authors = [{name = "AutoGenerated"}]
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
dev = [
    "pytest>=7.4",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
